profile: workload

storage:
  primary:
    type: file
    url: "config/usecases/data/runtime/07-workload-and-device-identity/kubernetes-service-account-token-exchange.json"

server:
  listen: "0.0.0.0:8443"
  public_base_url: "https://id.example.com"

crypto:
  keyrings:
    - name: kube-workload-ca
      realm_id: kube
      purposes:
        - other:workload_ca
      signer:
        type: local

realms:
  - id: kube
    issuer: "https://id.example.com/realms/kube"
    display_name: "Kubernetes Workload Federation"
    clients:
      - client_id: "kube-token-exchanger"
        client_type: confidential
        token_endpoint_auth_method: private_key_jwt
        jwks:
          keys:
            - kty: EC
              crv: P-256
              kid: "kube-token-exchanger-signing"
              use: sig
              alg: ES256
              x: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
              y: "bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb"
        grant_types:
          - client_credentials
          - urn:ietf:params:oauth:grant-type:token-exchange
    protocols:
      workload:
        spiffe_workload_api: true
        x509_svid: true
        jwt_svid: true
        short_lived_credentials: true
        rats_eat: true
        token_exchange: true
        workload_ca_key_ref: "kube-workload-ca"
      oauth:
        private_key_jwt:
          enabled: true
        mtls:
          enabled: true
        tokens:
          access_token_format: jwt
          access_token_ttl_seconds: 300
        resource_servers:
          - audience: "api://workloads/kubernetes"
            resources:
              - "https://id.example.com/workload/v1"
            scopes:
              - workload.attest
              - workload.svid
              - workload.exchange
            introspection_client_ids:
              - kube-token-exchanger
            require_sender_constraint: true
    policy:
      default_decision: deny
      bundles:
        - name: authenticated-read
          source: "file://../../policies/authenticated-read.json"
          mode: enforce
